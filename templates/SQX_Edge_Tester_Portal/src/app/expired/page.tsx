export default function ExpiredPage() {
  return (
    <main className="shell">
      <section className="panel hero">
        <p className="eyebrow">Access review required</p>
        <h1>Tester access is not active</h1>
        <p>
          Expired, denied or blocked testers must not reach Pro surfaces. The final copy and support links belong to
          the private commercial process.
        </p>
      </section>
    </main>
  );
}

