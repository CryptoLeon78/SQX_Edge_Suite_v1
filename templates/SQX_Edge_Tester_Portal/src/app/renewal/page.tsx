export default function RenewalPage() {
  return (
    <main className="shell">
      <section className="panel hero">
        <p className="eyebrow">Manual renewal</p>
        <h1>Renewal is pending operator approval</h1>
        <p>
          T6 will replace this placeholder with the verified continue, deny or block flow. No automatic renewal is
          allowed in the bootstrap.
        </p>
      </section>
    </main>
  );
}

