# T10ajh Cloudflare First Deploy Readiness

## Objective

T10ajh-readiness verifies that the local Cloudflare Worker package is ready for the first deploy gate, without running the deploy command and without creating any tester surface.

This phase does not create a Worker, deployment, Access policy, tester URL or tester account.

## Approval Status

```text
NO_DEPLOY_EXECUTED_EXACT_APPROVAL_REQUIRED
```

The exact approval phrase required by T10ajg has not been provided in this message, so the deploy command remains blocked.

Required phrase before the real first deploy:

```text
Autorizo T10ajh: ejecutar exactamente `npm exec --yes -- wrangler deploy --name sqx-edge-tester-portal-preview` desde `templates/SQX_Edge_Tester_Portal` despues de `npm run cf:build`, sin compartir URL tester y con inspeccion/cleanup inmediato si aparece una superficie publica no aceptada.
```

## Preflight Evidence

```text
npm run proof:cloudflare-first-deploy-approval-gate
result=GO_CLOUDFLARE_FIRST_DEPLOY_APPROVAL_GATE_READY_NO_PROVIDER_ACTION

npm exec --yes -- wrangler whoami
result=authenticated_redacted
raw_account_details_committed=false

npm exec --yes -- wrangler deployments list --name sqx-edge-tester-portal-preview --json
result=worker_not_found
cloudflare_error_code=10007

npm exec --yes -- wrangler versions list --name sqx-edge-tester-portal-preview --json
result=worker_not_found
cloudflare_error_code=10007

npm exec --yes -- wrangler secret list --name sqx-edge-tester-portal-preview --format json
result=worker_not_found

npm install
result=dependencies_installed_locally_package_lock_created

npm run cf:build
result=GO_OPENNEXT_CLOUDFLARE_BUILD_SUCCEEDED

npm audit --audit-level=high
result=GO_NO_HIGH_OR_CRITICAL_VULNERABILITIES
moderate_findings=2
moderate_fix_requires_breaking_force_update=true
```

The local build produced the OpenNext Worker bundle under ignored output directories. Those generated artifacts are not committed.

## Package Readiness

- `package-lock.json` is now committed so the tester portal template can use `npm ci` for reproducible installs.
- `next-env.d.ts` and `tsconfig.json` were aligned with the Next.js 16 build output generated by `next build`.
- `cf:build` remains local-only.
- No `cf:deploy`, `deploy`, `delete`, `upload` or provider mutation script was added.

## Result

```text
GO_CLOUDFLARE_FIRST_DEPLOY_READY_EXACT_APPROVAL_REQUIRED_NO_PROVIDER_MUTATION
```

The next real action is still the first controlled Cloudflare Worker deploy, but only after the exact approval phrase above.

## Security Boundary Preserved

- No Cloudflare Worker was created.
- No Cloudflare deployment was created.
- No Cloudflare version was uploaded.
- No Cloudflare Access application was created.
- No Cloudflare Access policy was created.
- No GitHub repository was connected to Cloudflare.
- No Cloudflare token or account ID was committed.
- No raw Wrangler account output was committed.
- No tester URL was published.
- No tester accounts were created.
- No tester emails were committed.
- No renewal emails were sent.
- No production database was connected.

## Next Gate

```text
T10aji_execute_first_worker_deploy_only_after_exact_approval
```

T10aji may execute the exact T10ajg command only after Ivan provides the exact approval phrase. If the first deploy creates an unsafe or uninspectable surface, cleanup must happen immediately according to T10ajg.

## Verification

T10ajh-readiness is accepted when:

- this document exists
- `scripts/cloudflare-first-deploy-readiness-proof.mjs` exists
- `package.json` exposes `proof:cloudflare-first-deploy-readiness`
- `package-lock.json` exists and pins the Cloudflare build toolchain
- `npm run cf:build` succeeds locally
- the proof returns `GO_CLOUDFLARE_FIRST_DEPLOY_READY_EXACT_APPROVAL_REQUIRED_NO_PROVIDER_MUTATION`
- no deploy/upload/delete script is added
- no provider token, account ID, tester email or tester URL appears in tracked public files
- static tests and full pytest pass
- `git diff --check` passes
